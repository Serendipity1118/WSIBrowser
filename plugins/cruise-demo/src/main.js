// Cruise Demo - site page part. The work happens in the worker; the page
// only announces itself so the badge and logs show the plugin is active.
WSI.log(`cruise-demo page on ${location.pathname}`);
