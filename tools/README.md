# tools/

| スクリプト | 用途 |
|---|---|
| `sync_wsi_samples.ps1` | Chrome 版 WSI リポジトリの `samples/` から公開サンプルを `plugins/samples/` に取り込む |
| `check_no_firebase.ps1` | Firebase 関連と既存アプリのバックエンド識別子がリポジトリに含まれていないことを検査する (CI でも実行) |
